let isPalindromeRecur = (lst) =>
    let {
        const check = (i, j) =>
            if (i >= j) then true
            else if (lst[i] != lst[j]) then false
            else check(i + 1, j - 1)
    } in check(0, len(lst) - 1)

// Ejemplo:
print(isPalindromeRecur([1, 2, 3, 2, 1])) // Retorna true
