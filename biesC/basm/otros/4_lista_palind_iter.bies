let isPalindromeIter = (lst) =>
    let {
        const loop = (i, j) =>
            if (i >= j) then true
            else if (lst[i] != lst[j]) then false
            else loop(i + 1, j - 1)
    } in loop(0, len(lst) - 1)

// Ejemplo:
print(isPalindromeIter([1, 2, 3, 2, 1])) // Retorna true
